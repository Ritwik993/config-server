# Spring Cloud Config Server – Configuration Repository

This repository contains **YAML configuration files** used by a **Spring Cloud Config Server**.
These configurations are centrally managed and shared across multiple microservices.

## 📌 Purpose

This repository is used to:
- Externalize configuration from microservices
- Centralize configuration management
- Support multiple environments (dev, test, prod)
- Enable runtime configuration refresh


